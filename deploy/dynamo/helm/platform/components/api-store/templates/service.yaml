apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynamo-api-store.fullname" . }}
  labels:
    {{- include "dynamo-api-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dynamo-api-store.selectorLabels" . | nindent 4 }} 